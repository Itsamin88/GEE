# Core dependencies. Install with:  pip install -r requirements.txt
#
# Everything the crawler NEEDS to run is here. Optional capabilities live in
# requirements-optional.txt; without them the program still runs and records
# what it could not do.

httpx>=0.27,<1.0            # async HTTP with connection pooling and timeouts
beautifulsoup4>=4.12        # HTML and XML parsing
lxml>=5.0                   # the parser BeautifulSoup uses for HTML and XML
openpyxl>=3.1               # reading and writing the Stage 1 workbook
python-docx>=1.1            # .docx documents, and reading the plan
pypdf>=4.0                  # PDF text, metadata and embedded images
pillow>=10.0                # image dimensions and format checks
PyYAML>=6.0                 # configuration and the field schema
